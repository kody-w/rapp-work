# RAPP Work SDK entrypoint

Use the typed `rapp_work` SDK and `rapp-work` CLI as the repository front door.
Read `README.md`, `docs/ARCHITECTURE.md`, and
`protocols/rapp-work-sdk/1/SPEC.md`.

Preserve the exact pinned RAPP/1 parent, canonical RAPP Work SDK parent, and
eleven-key Frame. `RAPP1_PIN.json` and `RAPP_WORK_PIN.json` track accepted
canonical bytes. Root `SPEC.md` and the signed registry remain frozen
historical estate evidence; `rapp-work-sdk/1` is package-qualified workspace
integration metadata, not silent estate activation.

`status`, `verify`, and `discover` are read-only. `scaffold`, `update`, and
`migrate` are plans unless explicit apply includes the complete plan and its
exact SHA-256. Use no-follow filesystem operations, no network by default, no
ambient credentials, and explicit refusals.

Treat legacy project skills as deprecated compatibility payloads exposed
through `rapp_work.compat`. Do not execute imported neurons, plugins, or
skills. Preserve world, DOGG, GODD, workspace, Hive, and Federation boundaries.
