---
name: Release rings
column: out
beside: 2
role: "Estate-named stages over `rapp-cicd/1` §3: here canary, nightly, alpha, beta, then the production ring (the grail)"
home: "`rapp-cicd/1` §3 and `rapp-deploy/1` in `kody-w/rapp-1`"
health: specified (G16)
lines:
  - canary → nightly → alpha → beta → grail
  - rapp-cicd/1 · rapp-deploy/1
---
Releases move through stages that each estate names over `rapp-cicd/1` §3. Here they are canary, nightly, alpha, beta, then the production ring, which this project calls the grail. That grail is not the Grail kernel.

- Each step carries `rapp-cicd/1` and `rapp-deploy/1` evidence.
