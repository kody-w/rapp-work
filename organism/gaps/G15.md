---
id: G15
gap: No estate declares the Brainstem's kernel
home: RAPP/1 §§11.1, 13.3; `KERNEL_PIN.json` in `kody-w/RAPP`
fix: "The estate owner signs a `grail-kernel` entry for each release scope: the LTS scope, which pins `brainstem-v0.6.9` for good, and one successor scope per newer kernel (`brainstem-v0.6.16` now); a scope is never rebound. Both scopes' candidate release manifests are prepared, unsigned, on `kody-w/rapp-estate` branch `experimental/rapp1-distributed-hive`"
status: proposed
phase: 4
who: estate owner
blocks: estate
---
RAPP/1 lets an estate declare which Grail kernel each release scope pins, with a signed `grail-kernel` registry entry. No estate does this for the Brainstem yet. Its kernel has two channels, by design: LTS, which RAPP pins in its unsigned `KERNEL_PIN.json` (`kody-w/rapp-installer@brainstem-v0.6.9`), and the newest `brainstem-v*` tag (`brainstem-v0.6.16`). The estate kit prepared both scopes there, unsigned: candidate release manifests for LTS (`rapp-1-lts-2026.09`, which pins `kody-w/rapp-1` at the rev-17 draft) and for the newest kernel, each listing the repositories it pins and those it leaves out. Its owner action asks the estate owner to sign one `grail-kernel` entry per scope, with rev-17's `release-pin` entries, as `registry_seq` 3 of the kody-w estate's registry; those entries are not drafted on the branch. This is a drift finding.
