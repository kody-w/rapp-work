---
id: G16
gap: No estate has activated canonical `rapp-work/1`
home: Canonical `rapp-work/1` §1
fix: "An estate appends the canonical `rapp-work/1` protocol entry, its seven `work.*` kinds and a `rapp-hive/1` pin; the kody-w estate already pins `rapp-cicd/1` and `rapp-deploy/1`"
status: open
phase: 4
who: estate owner
blocks: organization
---
Canonical `rapp-work/1` is specified, but no estate has adopted it: none has appended its protocol entry, its seven `work.*` kinds and a `rapp-hive/1` pin. The kody-w estate's signed registry (`kody-w/rapp-map` `ecosystem-spec.json`, `registry_seq` 2) already pins `rapp-cicd/1` and `rapp-deploy/1`. This repository's own `registry.json` is historical evidence, not an adoption. So organizations, their Hive vectors and their release evidence are specified, not active. This is a drift finding.
