---
id: G4
gap: SDK migration refuses repository-seeded Hives and long world ids
home: "`rapp-work-sdk/1` §10; the 64-character world id cap in `rapp-hive/1` `schema.json` `$defs/label` and the SDK's `migration.py`"
fix: Pointer-only successors; world ids up to 128 characters
status: open
phase: 3
who: spec owner
blocks: workspaces
---
SDK migration refuses a Hive that was seeded from a repository, and refuses world ids over 64 characters. That cap lives in `rapp-hive/1`'s `schema.json` (`$defs/label`) and in the SDK's `migration.py`. The proposed fix uses pointer-only successors and allows world ids up to 128 characters.
