---
from: outside-knowledge
to: hive
what: An old Hive's signed join requests, byte for byte; everything else through a reference
authorized_by: The old signature, checked with RAPP/1's hash and signature math; the Hive's rules
home: Hive folder convention; `previous:`
health: experimental
arrow: in
label: joins
---
When an old Hive moves, its signed join requests are carried byte for byte, so nobody is stranded. Everything else stays where it is and is reached through a reference.
