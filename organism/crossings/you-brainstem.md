---
from: you
to: brainstem
what: "A request, and the Brainstem's answer; for a Hive change, a plan in plain words"
authorized_by: "Your request. The LTS kernel runs the agents its model calls at once; the Hive agent applies only a plan you confirm in a later turn, bound to its hash"
home: "`kody-w/rapp-installer` `rapp_brainstem/brainstem.py` (`run_tool_calls`); the Hive agent"
health: in force (the kernel runs the agents its model calls, without asking); the later-turn hash gate is experimental (the Hive agent)
arrow: both
label: ask · answer · confirm Hive plans
---
You ask, and your Brainstem answers. The LTS kernel runs the agents its model calls at once, without asking you first. RAPP Constitution IX's click-to-accept covers only the twin's action chips. The Hive agent goes further, as an experiment: it applies only a plan you confirm in a later turn, bound to the plan's hash.
